1. Describe the difference between a Class and an Object.

an object is an instance of a class. every object belongs to a class. objects live for a limited amount of time.


2. What keyword do you use to turn a normal Class into a Subclass? (choose one)
  a. implements
  b. extends
  c. uses
  d. sub

extends


3. Describe the difference between public and private?

private can only be accessed in the same class, but public can be used in other classes.


4. Can a subclass access a private method of its parent class? (choose one)
  a. Yes
  b. No

no


5. Define the term constructor.

a constructor doesn’t have a return value, and instantiates the member variables of the class when you create an object in that same class.

6. Can you access a static method of a class without making an instance of it? (choose one)
  a. Yes
  b. No

yes


7. Describe the purpose of overriding a method.

so you can have different purposes in the same method name?
